// ! Undefined adalah tipe data dalam JavaScript yang digunakan untuk mewakili variabel yang belum diberi nilai apa pun.
    // Kapan pun Anda mendeklarasikan sebuah variabel tanpa memberikan nilai apa pun, nilai undefined akan ditetapkan ke variabel tersebut

    let first_name;
    console.log(first_name); // * Output undefined

    // ? Atau 
    // Kita bisa menetapkan undefined ke variabel secara eksplisit sebagai berikut:
    let last_name = undefined;
        // ! Namun hal ini biasanya tidak disarankan, karena JavaScript memiliki nilai lain bernama null 
